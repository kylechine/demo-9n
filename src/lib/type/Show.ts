/**
 * The data structure of a single show.
 */

type Show = {
  drm:          boolean,
  episodeCount: number,
  image:        string,
  slug:         string,
  title:        string
};

export { Show };
